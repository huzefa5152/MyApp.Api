interface:
  display_name: "MyApp Importer Print Templates"
  short_description: "Match Importer layouts with its own merge fields"
  default_prompt: "Use $myapp-importer-print-templates to create a professional template matching my reference on feat/importer-ledger-receipts."
